.pytest_cache
dist
__pycache__/
*.py[cod]
*.egg-info
MUJOCO_LOG.TXT
wandb/
hrl_director_fixes_todo.md

# Raw Claude Code terminal-session transcripts saved into the repo root
# (e.g. 2026-07-14-135544-local-command-caveat....txt) -- not source/data.
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-[0-9]*.txt


# Claude Code session worktrees created inside the repo.
.claude/worktrees/

# Rendered mask-viz panels: ~120MB of gifs/npz/pngs regenerated from
# checkpoints by experiments/mask_viz/render_mask_viz.py.
experiments/mask_viz/out/

# Timestamped backups taken before editing a live watchdog TSV.
job_logs/*.bak_*

# Large intermediate artifacts under experiments/: extracted policy dumps
# (.npz, ~13MB each) and rendered videos (.mp4). Regenerated from the scripts
# beside them; archived at
# /bucket/DoyaU/vasilache/bucket/results/dreamerv3_experiments_artifacts/
# .npz and .gif are stripped from history (2026-09-01): 2.53GB of extracted
# policy dumps and rendered animations. Archived at
# /bucket/DoyaU/vasilache/bucket/results/dreamerv3_experiments_artifacts/
*.npz
*.gif
experiments/**/*.mp4
